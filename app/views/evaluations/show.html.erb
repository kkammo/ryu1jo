<h2>
	<%= @evaluation.id %>차 평가
</h2>
<!--
  <table class="table table-condensed">
    <% @evaluation.mappings.each do |mapping| %>
      <tr>
        <td><%= link_to 'n', mapping_path(mapping) %></td>
        <td></td>
      </tr>
    <% end %>
  </table>
-->

<%= form_tag map_evaluation_path(@evaluation), :method => 'get', :role => 'map'  do %>
	<div>
		각 피평가 그룹의 사람 수
		<%= number_field_tag :rateemem, params[:rateemem] %>
	</div>
	<div>
		각 평가자 그룹의 사람 수
		<%= number_field_tag :ratermem, params[:ratermem] %>
	</div>
    <button type="submit" class="btn btn-warning btn-xs">
        매핑하기
	</button>
<% end %>
<%= puts params %>